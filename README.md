The chat server should allow the following:
a) given a users.txt file as list of registered users with pswd, allow to connect to the server only via one connecion/client (so active and unactive users, one connection per active users is ensured)
b) every active user can braodcast a msg to all other actoive users
c) all active users can see other active users
d) all active users can msg other active users privateloy, a info message should be sent if the reciepent is currently inactive
e) all active users can create groups 
f) groups can be joined or left by active users
g) group messasging must be exclusive to members of a group
h) active users can go inactive by either logging out or disconnecting
i) inactive users should stay as part of the group and can reaccess the group after logging back in
